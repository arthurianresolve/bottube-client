[package]
name = "bottube-published-smoke"
version = "0.0.0"
edition = "2024"
publish = false

[dependencies]
bottube-client = "=0.1.0"
tokio = { version = "1.53.1", features = ["macros", "rt-multi-thread"] }

[profile.dev]
debug = 0
incremental = false

[workspace]
